import 'package:flutter/material.dart';

class CoffeeTime extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Container(
      child:Center(
        child: Text('This is the Coffee Time page'),
      ),
    );
  }
}